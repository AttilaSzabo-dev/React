import classes from "./AdItemTopSlim.module.css";

const AdItemTopSlim = (props) => {
  return (
    <div className={classes.adItem}>
      <div className={classes.adContent}></div>
    </div>
  );
};

export default AdItemTopSlim;
