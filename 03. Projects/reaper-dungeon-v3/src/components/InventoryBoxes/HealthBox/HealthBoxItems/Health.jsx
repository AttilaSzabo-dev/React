import Border from "../../../UI/Border";

import "./Health.scss";

const Health = () => {
  return <Border className="health-item--container common-cell--border">Health</Border>;
};

export default Health;
